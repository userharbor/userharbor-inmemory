[project]
name = "userharbor-inmemory"
version = "0.1.0"
description = "A minimal, contract-tested UserStore implementation and GitHub template for building custom UserHarbor storage integrations."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "SpaceShaman", email = "spaceshaman@tuta.io" }]
keywords = [
  "auth",
  "authentication",
  "authorization",
  "login",
  "registration",
  "security",
  "sessions",
  "signup",
  "user-management",
  "users"
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3.15",
]
dependencies = [
  "userharbor>=0.7.0",
]

[project.urls]
Documentation = "https://userharbor.github.io/userharbor"
Homepage = "https://github.com/userharbor/userharbor-inmemory"
Repository = "https://github.com/userharbor/userharbor-inmemory"

[dependency-groups]
dev = [
  "pytest>=9.1.0",
  "pytest-cov>=7.1.0",
]

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
